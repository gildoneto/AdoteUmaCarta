<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="correios.natal.adoteumacarta" >

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:label="@string/label_principal"/>

        <activity android:name=".EscolaActivity"
            android:label="@string/label_escola">
        </activity>

        <activity android:name=".CartaActivity"
            android:label="@string/label_carta">
        </activity>
        <activity android:name=".CadastroActivity" />


    </application>

</manifest>